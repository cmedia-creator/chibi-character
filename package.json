{
  "name": "chibi-character",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "pixi.js": "^8.20.0"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vite": "^8.2.0",
    "wrangler": "^4.0.0"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}
